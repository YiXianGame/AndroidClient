<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_margin="10dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">
<ImageView
    android:id="@+id/head_image"
    android:layout_width="50dp"
    android:layout_height="50dp"
    app:layout_constraintTop_toTopOf="parent"
    app:layout_constraintLeft_toLeftOf="parent"
    >
</ImageView>
<Button
    android:id="@+id/change_pos_button"
    android:layout_width="50dp"
    android:layout_height="30dp"
    app:layout_constraintTop_toBottomOf="@id/head_image"
    app:layout_constraintLeft_toLeftOf="@id/head_image"
    app:layout_constraintRight_toRightOf="@id/head_image"
    android:text="@string/change_pos"
    android:textAppearance="@style/TextAppearance.YiXian.Body4"
    />
</androidx.constraintlayout.widget.ConstraintLayout>